# Raspberry Pi 3/4 notes

### UART

By default, the UART is configured to run at 115200 baud rate on the following GPIO pins:
```
TX = 14
RX = 15
```

### I2C

The I2C runs at 100 MHz and is configured for the following GPIO pins:
```
SDA = 2
SCL = 3
```